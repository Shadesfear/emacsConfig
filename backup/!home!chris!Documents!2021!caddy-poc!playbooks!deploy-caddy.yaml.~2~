- hosts: all
  gather_facts: true
  become: true

vars_prompt:
  - name: "platform_domain"
    prompt: "Enter platform domain"
  - name: "ssl_email"
    prompt: "SSL Email  Address"
    default: "mmu@2021.ai"
  - name: "Port"
    prompt: "Enter port"
    default: "8000"

  roles:
    - init
    - docker
    - caddy
